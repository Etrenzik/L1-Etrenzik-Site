# ─────────────────────────────────────────────────
# L1-ETRENZIK LITIGATION WORKSPACE - Environment
# ─────────────────────────────────────────────────
# DRAFT WORK PRODUCT ONLY — NOT LEGAL ADVICE
# All outputs require attorney review before use.
# ─────────────────────────────────────────────────

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/l1_etrenzik?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# OpenAI-compatible API
LLM_API_KEY="sk-your-key-here"
LLM_BASE_URL="https://api.openai.com/v1"
LLM_MODEL="gpt-4o"
LLM_EMBEDDING_MODEL="text-embedding-3-small"

# API server
API_PORT=4000
API_HOST="0.0.0.0"
API_CORS_ORIGIN="http://localhost:3000"

# Next.js web
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Worker
WORKER_CONCURRENCY=5
WORKER_QUEUE_PREFIX="l1-etrenzik"

# File storage
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE_MB=100

# Auth (placeholder — implement with NextAuth or similar)
AUTH_SECRET="change-me-in-production"
JWT_SECRET="change-me-in-production"

# Logging
LOG_LEVEL="info"
AUDIT_LOG_ENABLED="true"

# Environment
NODE_ENV="development"
